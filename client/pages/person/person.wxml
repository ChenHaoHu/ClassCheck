<!--pages/person/person.wxml-->

<i-panel title="个人中心">
    <i-input value="{{ value1 }}" title="名字" autofocus placeholder="名字" />
    <i-input value="{{ value2 }}" type="number"maxlength='11' title=" 学号" placeholder="请输入学号" />
    <i-input value="{{ value3 }}" type="textarea" title="地址" placeholder="请输入地址(最多50字)" maxlength="50" />
  <i-input value="{{ value4 }}" type="textarea" title="个性签名" placeholder="个性签名" maxlength="50" />
</i-panel>